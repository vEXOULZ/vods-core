---
'@vexoulz/vods-core': minor
---

`vodThumbnail(vod)` and `watchPath(vod, t?)`: the thumbnail and watch-page path the vods site shows for a VOD, so
other sites linking to a VOD (the stream card on vexoulz.net) pick the same ones.
